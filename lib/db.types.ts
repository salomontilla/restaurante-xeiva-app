/**
 * ARCHIVO GENERADO — no editar a mano.
 *
 * Se regenera con `pnpm gen:types` (requiere SUPABASE_DB_URL en el entorno) después
 * de CADA migración. Mientras no se haya generado contra la base real, este stub
 * permite que el proyecto compile.
 *
 * Ver README.md → "Base de datos".
 */

/* eslint-disable @typescript-eslint/no-explicit-any */
export type Database = any;
